import { FilterCollaboratorPipe } from './filter-collaborator.pipe';

describe('FilterCollaboratorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCollaboratorPipe();
    expect(pipe).toBeTruthy();
  });
});
